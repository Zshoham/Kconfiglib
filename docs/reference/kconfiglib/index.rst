Kconfiglib API reference
=========================

This reference is generated by Sphinx directly from the inline documentation
in :mod:`kconfiglib`. It is the authoritative API-level description for
configuration parsing, values, expressions, and generated output.

.. automodule:: kconfiglib
   :no-members:

The reference is split across the pages below. The core model classes describe
the parsed configuration tree; the remaining pages cover the preprocessor,
errors, expression helpers, and module-level constants.

.. toctree::
   :maxdepth: 2

   kconfig
   symbol
   choice
   menunode
   preprocessor-errors
   expressions
   constants
